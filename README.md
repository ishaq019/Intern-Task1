# MERN User + Address CRUD

## Run backend
cd backend
npm install
npm run dev

## Run frontend
cd frontend
npm install
npm run dev

MongoDB must be running locally, or replace MONGO_URI in backend/.env.
"# Intern-Task1" 
